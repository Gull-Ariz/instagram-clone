<% @follower_suggestions = User.where.not id: current_user.followeds.map{ |f| f['user_id'] } << current_user.id %>
$('#follower-suggestions').html("<%= j render 'shared/followers_suggestion', collection: @follower_suggestions%>");
